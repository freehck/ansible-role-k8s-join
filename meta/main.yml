---
galaxy_info:
  author: Dmitrii Kashin
  description: join nodes to kubernetes cluster
  role_name: k8s_join
  license: MIT

  min_ansible_version: 2.7

  platforms:
    - name: Ubuntu
      versions:
        - bionic

  galaxy_tags:
    - kubernetes
    - k8s
    - join
    - cluster

dependencies: []
# this role suggests it's applied to host with initialized k8s cluster
